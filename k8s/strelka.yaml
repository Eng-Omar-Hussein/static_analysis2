apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: strelka-argo
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/AhmedMaged77/static_analysis.git'
    targetRevision: HEAD
    path: strelka
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: strelka
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
